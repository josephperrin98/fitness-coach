# Copy to .env for local development. In a Codespace, set these as
# GitHub Codespaces secrets instead; .env is then unnecessary.
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:8765/callback
# FITNESS_DATA_DIR=.data
